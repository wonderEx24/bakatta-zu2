using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ImpressionManager : MonoBehaviour
{/*
    public int npcCount = 30;
    public float initialReachChance = 0.5f;
    public float spreadChance = 0.3f;
    public int spreadDepth = 3;

    //public Text impressionText;


    //private List<NPC> npcs = new List<NPC>();
    //private HashSet<NPC> seenNPCs = HashSet<NPC>();
    // Start is called before the first frame update
    void Start()
    {
        npcs.AddRange(FindObjectsOfType<NPC>());
    }

    public void PlayerPosted()
    {
        seenNPCs.Clear();

        foreach (var npc in npcs)
        {

            if (Random.value < initialReachChance)
            {
                SpreadTo(npc, spreadDepth);
            }
        }
    }

    int realCount = seenNPCs.Count;
    int scaledCount = Mathf.RoundToInt(realCount * scaleFactor);

    //impressionText.text = $"インプレッション数: {scaledCount:NO}";
    //Debug.Log("");
ß
    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
